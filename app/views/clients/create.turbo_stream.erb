<%= turbo_stream.update 'toasts', partial: 'components/layout/toast_messages' %>

<%= turbo_stream.replace 'client_form', partial: 'form', locals: { client: Client.new } %>
<%= turbo_stream.append 'clients', partial: 'client', locals: { client: @client } %>
