<!DOCTYPE html>
<html class="h-full bg-background text-muted-foreground transition-colors <%= cookies[:ui_theme] %>">
  <head>
    <title>SmarterGood</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="flex min-h-screen h-full w-full flex-row overflow-hidden">
    <%= render 'components/layout/toast_messages' %>
    <%= render 'components/layout/sidebar' %>
    <main class='h-full w-full flex flex-col bg-muted'>
      <%= render 'components/layout/header' %>
      <%= render 'components/layout/mobile_header' %>
      <div class='h-full w-full min-w-[350px] rounded-s-md overflow-y-auto'>
        <%= yield %>
      </div>
    </main>
  </body>
</html>
